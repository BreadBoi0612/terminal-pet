[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "terminal-pet"
version = "0.1.0"
description = "A terminal pet that reacts to Linux system stats"
requires-python = ">=3.8"
dependencies = ["psutil"]

[project.scripts]
pet = "pet.main:run"

